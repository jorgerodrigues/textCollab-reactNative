import React, { useState } from 'react';
import { StyleSheet, TextInput, View, Text } from 'react-native';

const InlineText = (props) => {
  const splitStringIntoPartsPerUser = () => {};

  return <Text></Text>;
};

export default InlineText;
